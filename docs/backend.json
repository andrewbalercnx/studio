{
  "entities": {
    "UserProfile": {
      "title": "User Profile",
      "description": "Stores user-specific information.",
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the user, matches Auth UID."
        },
        "email": {
          "type": "string",
          "format": "email",
          "description": "The user's email address."
        },
        "isAdmin": {
            "type": "boolean",
            "description": "Whether the user has administrative privileges."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp when the profile was created."
        }
      },
      "required": ["id", "email", "createdAt"]
    },
    "ChildProfile": {
      "title": "Child Profile",
      "description": "Stores information about a child using the app.",
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the child."
        },
        "displayName": {
          "type": "string",
          "description": "The child's chosen display name."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp when the profile was created."
        },
        "estimatedLevel": {
          "type": "number",
          "description": "AI-estimated reading/writing level (e.g., 1-5)."
        },
        "favouriteGenres": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "List of genres the child enjoys (e.g., 'funny', 'magical')."
        },
        "favouriteCharacterTypes": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Common character types the child likes (e.g., 'self', 'pet')."
        },
        "preferredStoryLength": {
          "type": "string",
          "enum": [
            "short",
            "medium",
            "long"
          ],
          "description": "The typical story length the child prefers."
        },
        "helpPreference": {
          "type": "string",
          "enum": [
            "more_scaffolding",
            "balanced",
            "independent"
          ],
          "description": "How much guidance the child prefers."
        }
      },
      "required": [
        "id",
        "displayName",
        "createdAt"
      ]
    },
    "StorySession": {
      "title": "Story Session",
      "description": "Represents a single story creation session for a child.",
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the story session."
        },
        "childId": {
          "type": "string",
          "description": "Reference to the child who owns this session."
        },
        "status": {
          "type": "string",
          "enum": [
            "in_progress",
            "completed"
          ],
          "description": "The current status of the story."
        },
        "currentPhase": {
          "type": "string",
          "description": "The current phase of the story creation flow (e.g., 'warmup', 'planBeats')."
        },
        "currentStepIndex": {
          "type": "number",
          "description": "The index of the current step within the current phase."
        },
        "storyTitle": {
          "type": "string",
          "description": "The title of the story."
        },
        "storyVibe": {
          "type": "string",
          "description": "The chosen vibe or genre for the story."
        },
        "characters": {
          "type": "array",
          "items": {
            "$ref": "#/entities/Character"
          },
          "description": "An array of characters in the story."
        },
        "beats": {
          "type": "array",
          "items": {
            "$ref": "#/entities/StoryBeat"
          },
          "description": "The planned story beats or plot points."
        },
        "finalStoryText": {
          "type": "string",
          "description": "The full, final generated story text."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time"
        },
        "promptConfigId": {
          "type": "string",
          "description": "The ID of the prompt config used for this session."
        },
        "promptConfigLevelBand": {
            "type": "string",
            "description": "The level band of the prompt config used for this session."
        },
        "storyTypeId": {
          "type": "string",
          "description": "Reference to storyTypes/{id}."
        },
        "storyPhaseId": {
          "type": "string",
          "description": "Reference to storyPhases/{id}."
        },
        "arcStepIndex": {
          "type": "number",
          "description": "0-based index into the story type's arcTemplate.steps array."
        }
      },
      "required": [
        "id",
        "childId",
        "status",
        "currentPhase",
        "currentStepIndex",
        "createdAt",
        "updatedAt"
      ]
    },
    "Character": {
      "title": "Story Character",
      "description": "A character within a story session.",
      "type": "object",
      "properties": {
        "name": {
          "type": "string"
        },
        "type": {
          "type": "string",
          "enum": [
            "self",
            "friend",
            "family",
            "pet",
            "imaginary"
          ]
        },
        "traits": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "goal": {
          "type": "string"
        }
      },
      "required": ["name", "type"]
    },
    "StoryBeat": {
        "title": "Story Beat",
        "description": "A single plot point or section of the story.",
        "type": "object",
        "properties": {
            "label": {
                "type": "string",
                "description": "A machine-readable label for the beat (e.g., 'start', 'problem')."
            },
            "childPlanText": {
                "type": "string",
                "description": "The child's one or two sentence plan for this beat."
            },
            "draftText": {
                "type": "string",
                "description": "The AI-drafted paragraph for this beat."
            }
        },
        "required": ["label"]
    },
    "ChatMessage": {
      "title": "Chat Message",
      "description": "A single message in a story session conversation.",
      "type": "object",
      "properties": {
        "id": {
          "type": "string"
        },
        "sender": {
          "type": "string",
          "enum": [
            "child",
            "assistant",
            "system"
          ]
        },
        "text": {
          "type": "string"
        },
        "choices": {
          "type": "array",
          "items": {
            "$ref": "#/entities/Choice"
          }
        },
        "createdAt": {
          "type": "string",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "sender",
        "text",
        "createdAt"
      ]
    },
    "Choice": {
      "title": "Chat Choice",
      "description": "A tappable choice presented to the child in the chat.",
      "type": "object",
      "properties": {
        "label": {
          "type": "string",
          "description": "The text displayed to the child."
        },
        "value": {
          "type": "string",
          "description": "The machine-readable value sent back when tapped."
        }
      },
      "required": ["label", "value"]
    },
    "PromptConfig": {
      "title": "Prompt Configuration",
      "description": "Configuration for Genkit prompts, driving the conversation for a specific phase and language level.",
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the prompt configuration."
        },
        "phase": {
          "type": "string",
          "description": "The story creation phase this config applies to (e.g., 'warmup', 'planBeats')."
        },
        "languageLevel": {
          "type": "string",
          "enum": ["low", "medium", "high"],
          "description": "Optional language complexity level this config targets."
        },
        "systemPrompt": {
          "type": "string",
          "description": "High-level behavior instructions for the Genkit LLM."
        },
        "modeInstructions": {
          "type": "string",
          "description": "Detailed instructions for the current phase."
        },
        "additionalContextTemplate": {
          "type": "object",
          "description": "A template for injecting dynamic data into the prompt. Can contain placeholders like {{childName}}."
        },
        "allowedChatMoves": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Defines the types of responses the AI is allowed to make (e.g. 'ask_short_question')."
        }
      },
      "required": ["id", "phase", "systemPrompt", "modeInstructions"]
    },
    "StoryPhase": {
        "title": "Story Phase",
        "description": "A phase of the story interaction.",
        "type": "object",
        "properties": {
            "id": { "type": "string" },
            "name": { "type": "string" },
            "phaseType": { "type": "string", "enum": ["warmup", "storyBeat", "ending"] },
            "description": { "type": "string" },
            "choiceCount": { "type": "number" },
            "allowMore": { "type": "boolean" },
            "status": { "type": "string", "enum": ["live", "draft"] },
            "orderIndex": { "type": "number" }
        },
        "required": ["id", "name", "phaseType", "choiceCount", "allowMore", "status", "orderIndex"]
    },
    "StoryType": {
        "title": "Story Type",
        "description": "A template for a story.",
        "type": "object",
        "properties": {
            "id": { "type": "string" },
            "name": { "type": "string" },
            "shortDescription": { "type": "string" },
            "ageRange": { "type": "string" },
            "status": { "type": "string", "enum": ["live", "draft"] },
            "tags": { "type": "array", "items": { "type": "string" } },
            "defaultPhaseId": { "type": "string" },
            "endingPhaseId": { "type": "string" },
            "levelBands": { "type": "array", "items": { "type": "string" } },
            "arcTemplate": {
                "type": "object",
                "properties": {
                    "steps": { "type": "array", "items": { "type": "string" } }
                }
            }
        },
        "required": ["id", "name", "shortDescription", "status", "tags", "defaultPhaseId", "endingPhaseId", "levelBands", "arcTemplate"]
    }
  },
  "auth": {
    "providers": [
      "anonymous",
      "email_and_password"
    ]
  },
  "firestore": {
    "/users/{userId}": {
        "schema": { "$ref": "#/entities/UserProfile" },
        "description": "Stores public profiles for users."
    },
    "/children/{childId}": {
      "schema": { "$ref": "#/entities/ChildProfile" },
      "description": "Stores individual child profiles."
    },
    "/storySessions/{storySessionId}": {
      "schema": { "$ref": "#/entities/StorySession" },
      "description": "Stores story creation sessions."
    },
    "/storySessions/{storySessionId}/messages/{messageId}": {
      "schema": { "$ref": "#/entities/ChatMessage" },
      "description": "Stores the sequence of messages for a given story session."
    },
    "/promptConfigs/{configId}": {
      "schema": { "$ref": "#/entities/PromptConfig" },
      "description": "Stores the prompt configurations that drive the AI's behavior."
    },
    "/storyPhases/{phaseId}": {
        "schema": { "$ref": "#/entities/StoryPhase" },
        "description": "Stores the configuration for story interaction phases."
    },
    "/storyTypes/{typeId}": {
        "schema": { "$ref": "#/entities/StoryType" },
        "description": "Stores templates for different types of stories."
    }
  }
}
